foreach ($line in (cat C:\Windows\System32\drivers\etc\hosts))
{
    if ($line -notmatch "^#|^$")
    {
        ($ip, $dnsname, $myalias, $rubbish) = [regex]::split($line.trim(),  '\s+')
        "{0}  {1}  {2}" -f $ip, $myalias, $dnsname
    }
}