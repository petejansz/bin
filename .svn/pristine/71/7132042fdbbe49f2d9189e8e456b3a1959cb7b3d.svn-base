$csv = import-csv "C:\Users\pjansz\Documents\pd\reg-users\orig.csv"

foreach ($player in $csv)
{
    try
    {
        $token = pdplayer -username $player.PlayerEmail -password 'RegTest6100' -hostname 'player.calottery.com' -port 443 -logintoken
        green ("{0}" -f $player.PlayerEmail) 
        #Write-Host ("{0}" -f $player.PlayerEmail) >> good.txt
        
    }
    catch [Exception]
    {
        Write-Host ("NG: {0}" -f $player.PlayerEmail)
    }
}